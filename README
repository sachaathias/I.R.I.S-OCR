To compile our prject:
	make

To run our project:
	./main  --XOR (to run XOR neural network)
		--img_trt <image_path> (to run binarisation and segmentation)
		--Train (to train the neural network)
		--start (to launch GUI)
		--help (to show commands)
