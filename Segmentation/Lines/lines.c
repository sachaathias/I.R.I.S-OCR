#include <stdio.h>
#include <stdlib.h>
#include <err.h>

int is_blank_line(SDL_Surface *image, int index) 
{
	int height = image -> height;
	int width = image -> width;
	int x = 0;
	int y = 0;
	Uint32 pixel = get_pixel
	while (
}

// Rajoute les lignes rouges
void split_lines(SDL_Surface *image)
{
	int height = image -> height;
	int width = image -> width;
}

