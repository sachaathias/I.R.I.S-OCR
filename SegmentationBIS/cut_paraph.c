#include <stdio>
#include "../Segmentation/cut_paraph.h"


/*Ici on decpoupe les paragraphes de l'image*/

void cut_paraph(image[][],size_t i, size_t j, indexs[])
{
	for (int height = 0; height < i ; height ++)
	{

}
